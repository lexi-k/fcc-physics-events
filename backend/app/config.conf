database {
    host = ${POSTGRES_HOST}
    port = ${POSTGRES_PORT}

    user = ${POSTGRES_USER}
    password = ${POSTGRES_PASSWORD}

    db = ${POSTGRES_DB}

    schema_file = ${?DATABASE_SCHEMA_FILE}
}
