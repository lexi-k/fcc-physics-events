<?xml version="1.0"?>
<configuration>
   <system.webServer>
      <defaultDocument enabled="true">    <!-- this line enables default documents for a directory -->
         <files>      
            <add value="index.php"/>     <!-- foo.htm is now the default document  -->                
         </files>
      </defaultDocument>
   </system.webServer>
</configuration>

